[Unit]
Description=onemount
After=network.target

[Service]
Type=exec
ExecStart=/usr/bin/onemount %f
ExecStopPost=/usr/bin/fusermount3 -uz %f
Restart=on-abnormal
RestartSec=3
RestartForceExitStatus=2

[Install]
WantedBy=default.target
